{
  "name": "lambda-screenshot",
  "version": "0.1.0",
  "bin": {
    "lambda-screenshot": "bin/lambda-screenshot.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.22.0",
    "esbuild": "^0.14.38",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.22.0",
    "constructs": "^10.0.0"
  }
}
